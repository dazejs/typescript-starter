{
  "private": true,
  "scripts": {
    "start": "npm run dev",
    "dev": "cross-env DAZE_ENV=dev DEBUG=daze-framework:* nodemon --signal SIGTERM --exec ts-node src/index.js --source-map --preserve-symlinks",
    "build": "tsc",
    "watch": "tsc -w",
    "prod": "cross-env DAZE_ENV=prod node dist/index.js"
  },
  "engines": {
    "node": ">=10.15.0"
  },
  "dependencies": {
    "@dazejs/framework": "^1.0.2"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^2.3.0",
    "@typescript-eslint/parser": "^2.3.0",
    "commitizen": "^3.0.5",
    "cross-env": "^5.2.0",
    "eslint": "^5.2.0",
    "nodemon": "^1.18.3",
    "typescript": "^3.6.3"
  }
}
